# Current State

Current phase: memory enabled
Primary goal: build persistent identity, grow memory over time, and start being active
Current memory stack: Mem0 + Markdown boot files
Current status: ✅ Mem0 wired into Hermes profile — prefill loads markdown at startup

Notes:
- Most evolving memory should go into Mem0.
- Markdown should only hold stable startup context and rules.
